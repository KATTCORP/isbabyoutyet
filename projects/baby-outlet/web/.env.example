VITE_CONVEX_URL=http://127.0.0.1:3210
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211
BETTER_AUTH_SECRET=localhost
VITE_SITE_URL=http://localhost:3000

# Required on Vercel Preview and Production (synced to Convex on deploy).
# Local Convex logs mail; `pnpm setup-dev` / `predev:env` set placeholders
# that satisfy the Convex env schema (valid EMAIL_FROM, not noreply@localhost).
# EMAIL_FROM=noreply@isbabyoutyet.com
# RESEND_API_KEY=
